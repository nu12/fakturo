%h1
  Category details

.container.w-50
  %table.table
    %thead
      %tr
        %th Info
        %th Value
    %tbody
      %tr
        %td Name
        %td= @category.name
      %tr
        %td Description
        %td= @category.description
      %tr
        %td # of sub-categories
        %td= @category.subcategories.count
      %tr
        %td # of expenses
        %td= @category.expenses.count
      %tr
        %td{colspan: "2"}
          = link_to 'Manage sub-categories', category_subcategories_path(@category), class: "btn btn-light form-control"
      %tr
        %td{colspan: "2"}
          = link_to 'Edit this category', edit_category_path(@category), class: "btn btn-light form-control"
      %tr
        %td{colspan: "2"}
          %button.btn.btn-danger.form-control{:type => "button", "data-bs-toggle" => "modal", "data-bs-target" => "#deleteModal"}
            Delete this category
  
= render partial: "layouts/delete_modal", locals: {element: "category", link: @category}

.accordion#accordionExpenses
  .accordion-item
    %h2.accordion-header
      %button.accordion-button.collapsed{:type => "button", "data-bs-toggle" => "collapse", "data-bs-target" => "#collapseOne", "aria-expanded" => "false", "aria-controls" => "collapseOne"}
        Expenses
    #collapseOne.accordion-collapse.collapse{"data-bs-parent" => "#accordionExpenses"}
      .accordion-body

        %table.table.table-sm.table-striped
          %thead
            %tr
              %th Date
              %th Description
              %th Value
              %th Comment
              %th Ignore
              %th Document
              %th Source
              %th Sub-category
              %th

          %tbody
            - @category.expenses.each do |expense|
              %tr
                %td= expense.date.strftime("%Y-%m-%d")
                %td= expense.description
                %td= format("%.2f", expense.value)
                %td= expense.comment
                %td= expense.ignore
                %td= expense.document.name
                %td= expense.document.source.name
                %td= expense.subcategory.name
                %td= link_to 'Show', category_expense_path(@category, expense)
