= form_for [@category, @subcategory] do |f|
  - if @subcategory.errors.any?
    #error_explanation
      %h2= "#{pluralize(@subcategory.errors.count, "error")} prohibited this subcategory from being saved:"
      %ul
        - @subcategory.errors.full_messages.each do |message|
          %li= message

  .field.mb-3
    = f.label :name, {class: "form-label"}
    = f.text_field :name, class: "form-control"

  .field.mb-3
    = f.label :description, {class: "form-label"}
    = f.text_field :description, class: "form-control"
  
  .actions.mb-3
    = f.submit 'Save', class: "form-control btn btn-fakturo btn-fakturo-action"
