= link_to 'New Expense', new_expense_path, {class: "btn btn-fakturo btn-fakturo-action float-end"}

%h1 Expenses

= render partial: "table", locals: {expenses: @expenses, show: %i[ statement source category subcategory ]}
