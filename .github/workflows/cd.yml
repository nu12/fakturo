name: Release
on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  cd:
    name: Release
    uses: nu12/templates/.github/workflows/go-release.yml@main
    with:
      ruby-version: .ruby-version
